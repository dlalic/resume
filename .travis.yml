language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
after_success:
  - if [ "$TRAVIS_RUST_VERSION" == "stable" ]; then
    cargo bench;
    fi