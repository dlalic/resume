name: resume
args:
    - config:
        long: config
        help: A resume yaml specification, see examples/resume.yaml
        takes_value: true
    - mode:
        long: mode
        help: Render the resume to a .tex file or the browser extension
        possible_values: [ tex, be ]
        takes_value: true