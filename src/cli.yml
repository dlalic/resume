name: resume
args:
    - config:
        value_name: FILE
        help: A resume yaml specification, see examples/resume.yaml
        takes_value: true
subcommands:
    - tex:
        about: Render the resume to a .tex file
    - sn:
        about: Publish the resume on social network